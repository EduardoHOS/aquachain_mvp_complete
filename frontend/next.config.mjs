/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // Ignora todos os erros do ESLint durante `next build`
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
  